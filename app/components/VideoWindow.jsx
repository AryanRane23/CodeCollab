// DEPRECATED: VideoWindow replaced by VideoCall. Keep this stub to avoid import errors.
import React from 'react';

export default function VideoWindow() {
  return (
    <div style={{ padding: 12, background: '#222', color: '#fff' }}>
      This component has been replaced by <strong>VideoCall</strong>. Remove usage or import <code>VideoCall</code> instead.
    </div>
  );
}
